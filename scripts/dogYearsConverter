/* My age */
const myAge = 29;
/*  */
let earlyYears = 2;
/* Multiply early years into their respective dog years */
earlyYears *= 10.5
/* */
let laterYears = myAge - 2;
/* Multiply the later years by  */
laterYears *= 4
/* Test point for loging earlyYears and laterYears. */
console.log(earlyYears);
console.log(laterYears);
/* Adds the two together */
let myAgeInDogYears = earlyYears + laterYears;
/* Test point for myAgeInDogYears */
console.log(myAgeInDogYears);
myName = "David Oikle".toLowerCase();
/* Test point for myName */
console.log(myName);
console.log(`My name is ${myName}. I am ${myAge} old in human years, which is ${myAgeInDogYears} years old in dog years.`)
